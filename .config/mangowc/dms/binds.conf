# Key Bindings
# key name refer to `xev` or `wev` command output,
# mod keys name: super,ctrl,alt,shift,none

# reload config
bind=SUPER,r,reload_config

# menu and terminal
bind=SUPER,space,spawn,dms ipc call spotlight toggle
bind=SUPER,Return,spawn,ghostty

# exit
# quit = terminate MangoWC
# killclient = terminate focused program
bind=CTRL+SHIFT,q,quit
bind=SUPER,q,killclient,

# switch window focus
bind=SUPER,Tab,focusstack,next
bind=ALT,Left,focusdir,left
bind=ALT,Right,focusdir,right
bind=ALT,Up,focusdir,up
bind=ALT,Down,focusdir,down

# swap window
bind=SUPER+SHIFT,Up,exchange_client,up
bind=SUPER+SHIFT,Down,exchange_client,down
bind=SUPER+SHIFT,Left,exchange_client,left
bind=SUPER+SHIFT,Right,exchange_client,right

# switch window status
bind=SUPER+SHIFT,g,toggleglobal,
bind=ALT,Tab,toggleoverview,
bind=ALT,backslash,togglefloating,
bind=ALT,a,togglemaximizescreen,
bind=ALT,f,togglefullscreen,
bind=ALT+SHIFT,f,togglefakefullscreen,
bind=SUPER,i,minimized,
bind=SUPER,o,toggleoverlay,
bind=SUPER+SHIFT,I,restore_minimized
bind=ALT,z,toggle_scratchpad

# scroller layout
bind=ALT,e,set_proportion,1.0
bind=ALT,x,switch_proportion_preset,

# switch layout
bind=SUPER,n,switch_layout
bind=SUPER,g,setlayout,grid
bind=ALT,c,setlayout,center_tile

# Clipboard (DMS 1.2)
bind=SUPER,c,spawn,dms cl copy
bind=SUPER,v,spawn,dms cl paste

# tag switch
bind=SUPER,Left,viewtoleft,0
bind=CTRL,Left,viewtoleft_have_client,0
bind=SUPER,Right,viewtoright,0
bind=CTRL,Right,viewtoright_have_client,0
bind=CTRL+SUPER,Left,tagtoleft,0
bind=CTRL+SUPER,Right,tagtoright,0

bind=Ctrl,1,view,1,0
bind=Ctrl,2,view,2,0
bind=Ctrl,3,view,3,0
bind=Ctrl,4,view,4,0
bind=Ctrl,5,view,5,0
bind=Ctrl,6,view,6,0
bind=Ctrl,7,view,7,0
bind=Ctrl,8,view,8,0
bind=Ctrl,9,view,9,0

# tag: move client to the tag and focus it
# tagsilent: move client to the tag and not focus it
# bind=Alt,1,tagsilent,1
bind=Alt,1,tag,1,0
bind=Alt,2,tag,2,0
bind=Alt,3,tag,3,0
bind=Alt,4,tag,4,0
bind=Alt,5,tag,5,0
bind=Alt,6,tag,6,0
bind=Alt,7,tag,7,0
bind=Alt,8,tag,8,0
bind=Alt,9,tag,9,0

# monitor switch
bind=alt+shift,Left,focusmon,left
bind=alt+shift,Right,focusmon,right
bind=SUPER+Alt,Left,tagmon,left
bind=SUPER+Alt,Right,tagmon,right

# gaps
bind=ALT+SHIFT,X,incgaps,1
bind=ALT+SHIFT,Z,incgaps,-1
bind=ALT+SHIFT,R,togglegaps

# movewin
bind=CTRL+SHIFT,Up,movewin,+0,-50
bind=CTRL+SHIFT,Down,movewin,+0,+50
bind=CTRL+SHIFT,Left,movewin,-50,+0
bind=CTRL+SHIFT,Right,movewin,+50,+0

# resizewin
bind=CTRL+ALT,Up,resizewin,+0,-50
bind=CTRL+ALT,Down,resizewin,+0,+50
bind=CTRL+ALT,Left,resizewin,-50,+0
bind=CTRL+ALT,Right,resizewin,+50,+0

# Mouse Button Bindings
# NONE mode key only work in ov mode
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=NONE,btn_middle,togglemaximizescreen,0
mousebind=SUPER,btn_right,moveresize,curresize
mousebind=NONE,btn_left,toggleoverview,1
mousebind=NONE,btn_right,killclient,0

# Axis Bindings
axisbind=SUPER,UP,viewtoleft_have_client
axisbind=SUPER,DOWN,viewtoright_have_client

# DMS IPC Bindings
# Core modals
bind=SUPER+SHIFT,n,spawn,dms ipc call notifications toggle
bind=SUPER+SHIFT,v,spawn,dms ipc call clipboard toggle
bind=SUPER,Escape,spawn,dms ipc call powermenu toggle
bind=SUPER+SHIFT,c,spawn,dms ipc call control-center toggle
bind=SUPER,d,spawn,dms ipc call dash toggle

# Audio controls
bind=NONE,XF86AudioRaiseVolume,spawn,dms ipc call audio volume increment 10
bind=NONE,XF86AudioLowerVolume,spawn,dms ipc call audio volume decrement 10
bind=NONE,XF86AudioMute,spawn,dms ipc call audio mute toggle
bind=NONE,XF86AudioMicMute,spawn,dms ipc call audio mic-mute toggle

# Brightness controls
bind=NONE,XF86MonBrightnessUp,spawn,dms ipc call brightness increment 5
bind=NONE,XF86MonBrightnessDown,spawn,dms ipc call brightness decrement 5

# Media controls
bind=NONE,XF86AudioPlay,spawn,dms ipc call mpris play-pause
bind=NONE,XF86AudioNext,spawn,dms ipc call mpris next
bind=NONE,XF86AudioPrev,spawn,dms ipc call mpris previous
bind=NONE,XF86AudioStop,spawn,dms ipc call mpris stop

# Screenshots
bind=SUPER,s,spawn,dms ipc call screenshot selection
bind=SUPER+SHIFT,s,spawn,dms ipc call screenshot full
bind=SUPER+ALT,s,spawn,dms ipc call screenshot window

# Appearance
bind=SUPER,t,spawn,dms ipc call theme toggle
# bind=SUPER+SHIFT,n,spawn,dms ipc call nightmode toggle

# Utility
bind=SUPER,l,spawn,dms ipc call lock
bind=SUPER,slash,spawn,dms ipc call keybinds toggle
bind=CTRL+SHIFT,Escape,spawn,dms ipc call processlist toggle
